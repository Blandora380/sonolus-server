import { createServerCreateRoomRouteHandler, } from '../routes/multiplayer/create.js';
import { createServerJoinRoomRouteHandler, } from '../routes/multiplayer/join.js';
export class SonolusMultiplayer {
    _getCreates;
    createHandler;
    joinHandler;
    _createRouteHandler;
    _joinRouteHandler;
    constructor(sonolus, getCreates) {
        this._getCreates = getCreates;
        this._createRouteHandler = createServerCreateRoomRouteHandler(this);
        this._joinRouteHandler = createServerJoinRoomRouteHandler(sonolus, this);
    }
    get creates() {
        return this._getCreates();
    }
}
