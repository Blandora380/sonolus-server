export const handleError = (response, res, localize) => {
    if (typeof response === 'number') {
        res.status(response).end();
        return true;
    }
    if (Array.isArray(response)) {
        const [code, text] = response;
        const body = {
            message: localize(text),
        };
        res.status(code).json(body);
        return true;
    }
    return false;
};
