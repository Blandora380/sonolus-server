import { Icon, LocalizationText, ServerInfo, Srl } from '@sonolus/core';
import { Localize } from '../../utils/localization.js';
import { PickOptions, ServerOptionsModel } from './options/option.js';
export type ServerInfoButtonModel = ServerInfoAuthenticationButtonModel | ServerInfoItemButtonModel | ServerInfoConfigurationButtonModel;
export type ServerInfoAuthenticationButtonModel = {
    type: 'authentication';
};
export type ServerInfoItemButtonModel = {
    type: 'room' | 'post' | 'playlist' | 'level' | 'replay' | 'skin' | 'background' | 'effect' | 'particle' | 'engine' | 'user';
    title?: LocalizationText;
    icon?: Icon;
    badgeCount?: number;
    infoType?: string;
    itemName?: string;
};
export type ServerInfoConfigurationButtonModel = {
    type: 'configuration';
};
export type ServerInfoModel<T extends ServerOptionsModel> = {
    title: LocalizationText;
    description?: LocalizationText;
    buttons: ServerInfoButtonModel[];
    configuration: {
        options: PickOptions<T>;
    };
    banner?: Srl;
};
export declare const toServerInfo: <T extends ServerOptionsModel>(localize: Localize, serverInfo: ServerInfoModel<T>, options: T) => ServerInfo;
