import Value from 'typebox/value';
export const parse = (json, schema) => {
    try {
        const value = JSON.parse(typeof json === 'string' ? json : json.toString('utf8'));
        if (!Value.Check(schema, value))
            return;
        Value.Clean(schema, value);
        return value;
    }
    catch {
        return;
    }
};
